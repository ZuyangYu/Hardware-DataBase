{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {"type": "timeseries", "title": "Chat turns by status", "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}, "targets": [{"expr": "sum by (status) (rate(hdb_chat_turns_total[$__rate_interval]))", "legendFormat": "{{status}}"}]},
    {"type": "timeseries", "title": "Chat p95 latency", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}, "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(hdb_chat_turn_duration_seconds_bucket[$__rate_interval])))", "legendFormat": "p95"}]},
    {"type": "timeseries", "title": "Queue depth", "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8}, "targets": [{"expr": "max by (queue) (hdb_queue_depth)", "legendFormat": "{{queue}}"}]},
    {"type": "timeseries", "title": "LLM p95 latency", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}, "targets": [{"expr": "histogram_quantile(0.95, sum by (le, provider) (rate(hdb_llm_duration_seconds_bucket[$__rate_interval])))", "legendFormat": "{{provider}}"}]}
  ],
  "schemaVersion": 39,
  "style": "dark",
  "title": "Hardware DataBase / Overview",
  "tags": ["hardware-database", "observability"],
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "browser",
  "version": 1
}
