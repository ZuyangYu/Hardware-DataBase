# 前端环境变量模板（Vite 只加载 VITE_ 前缀变量）
# 开发环境默认将 /api 代理到本机 8001；跨主机部署时请改为可达的 API 地址。
# VITE_API_PROXY_TARGET=http://127.0.0.1:8001
# 对话侧文档工具桥和会话附件默认开启；设为 false 显式关闭
# VITE_AGENT_DOCUMENT_TOOLS_ENABLED=false
# VITE_CHAT_ATTACHMENTS_ENABLED=true
